{
  "installCommand": "cd ../frontend-vite && npm install",
  "buildCommand": "cd ../frontend-vite && npm run build && mkdir -p dist && cp -r ../frontend-vite/dist/* dist/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}